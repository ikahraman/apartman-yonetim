@page "/admin/site-faturalama"

@attribute [Authorize(Roles = "SuperAdmin")]
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
        => Nav.NavigateTo("/admin/faturalar", replace: true);
}
